import { t as HookHandler } from "../../hooks-CSYsxDLm.js";

//#region src/hooks/bundled/boot-md/handler.d.ts
/** Gateway-startup hook that runs BOOT.md checks once per unique agent workspace. */
declare const runBootChecklist: HookHandler;
//#endregion
export { runBootChecklist as default };