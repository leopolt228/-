import { t as formatDocsLink } from "../../links-ClIwBcy4.js";
import { t as DEFAULT_ACCOUNT_ID } from "../../account-id-C7N4Rwku.js";
import { a as buildChannelConfigSchema } from "../../config-schema-DGcmKABe.js";
import { r as logVerbose, t as danger } from "../../globals-DBBT7Ru5.js";
import { l as mimeTypeFromFilePath } from "../../mime-De36NoRj.js";
import { n as getAgentScopedMediaLocalRoots } from "../../local-roots-BxhvvT09.js";
import { r as loadWebMediaRaw } from "../../web-media-wl1hy1PL.js";
import { n as firstDefined } from "../../allow-from-o-cfFFcK.js";
import { t as clearAccountEntryFields } from "../../config-helpers-CGWarYiR.js";
import "../../agent-media-payload-J6KzL54K.js";
import "../../runtime-env-BDC_axp1.js";
import { n as stripMarkdown } from "../../chunk-items-Bwws2oGs.js";
import "../../core-Bo6nGN10.js";
import { J as setSetupChannelEnabled, Q as splitSetupEntries } from "../../setup-wizard-helpers-BswN5Wen.js";
import "../../setup-BBJhG_GE.js";
import "../../web-media-DdHgGDGy.js";
import "../../media-mime-Bp89ch9k.js";
import { o as buildTokenChannelStatusSummary, r as buildComputedAccountStatusSnapshot } from "../../status-helpers-jGB19KP8.js";
import "../../channel-config-schema-CHISkkx7.js";
import { i as resolveLineAccount, n as normalizeAccountId, r as resolveDefaultLineAccountId, t as listLineAccountIds } from "../../accounts-DE_zW1FA.js";
import { a as resolveLineGroupConfigEntry, i as resolveExactLineGroupConfigKey, o as resolveLineGroupLookupIds, r as setLineRuntime, s as resolveLineGroupsConfig } from "../../quick-reply-fallback-xgoSKEeu.js";
import { i as LineConfigSchema, n as parseLineDirectives, r as LineChannelConfigSchema, t as hasLineDirectives } from "../../reply-payload-transform-afMkYOGZ.js";
import { _ as createNotificationBubble, a as datetimePickerAction, c as uriAction, d as createReceiptCard, f as createActionCard, g as createListCard, h as createInfoCard, i as createMediaPlayerCard, l as createAgendaCard, m as createImageCard, n as createAppleTvRemoteCard, o as messageAction, p as createCarousel, r as createDeviceControlCard, s as postbackAction, t as toFlexMessage, u as createEventCard } from "../../flex-templates-bFuD0vX3.js";
import { a as validateLineSignature, i as parseLineWebhookBody, n as createLineNodeWebhookHandler, o as downloadLineMedia, r as readLineWebhookRequestBody, s as normalizeAllowFrom, t as monitorLineProvider } from "../../monitor-CcM2HOL6.js";
import { a as buildPath, i as mergeHeaders, n as HTTPFetchClient, r as convertResponseToReadable, t as MessagingApiClient } from "../../messagingApiClient-DFqsQGxN.js";
import { t as probeLineBot } from "../../probe-BhjG-58x.js";
import { A as createButtonMenu, C as pushTemplateMessage, D as showLoadingAnimation, E as sendMessageLine, F as createImageCarouselColumn, I as createLinkMenu, L as createProductCarousel, M as createCarouselColumn, N as createConfirmTemplate, O as resolveLineChannelAccessToken, P as createImageCarousel, R as createTemplateCarousel, S as pushMessagesLine, T as replyMessageLine, _ as getUserProfile, a as extractLinks, b as pushLocationMessage, c as processLineMessage, d as createImageMessage, f as createLocationMessage, g as getUserDisplayName, h as createVideoMessage, i as extractCodeBlocks, j as createButtonTemplate, k as buildTemplateMessageFromPayload, l as createAudioMessage, m as createTextMessageWithQuickReplies, n as convertLinksToFlexBubble, o as extractMarkdownTables, p as createQuickReplyItems, r as convertTableToFlexBubble, s as hasMarkdownToConvert, t as convertCodeBlockToFlexBubble, u as createFlexMessage, v as pushFlexMessage, w as pushTextMessageWithQuickReplies, x as pushMessageLine, y as pushImageMessage, z as createYesNoConfirm } from "../../markdown-to-line-BzTbYYIA.js";
//#region node_modules/@line/bot-sdk/dist/messaging-api/api/messagingApiBlobClient.js
/**
* LINE Messaging API
* This document describes LINE Messaging API.
*
* The version of the OpenAPI document: 0.0.1
*
*
* NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
* https://openapi-generator.tech
* Do not edit the class manually.
*/
/**
* @see {@link LineBotClient} for a unified interface that wraps this client.
*/
var MessagingApiBlobClient = class {
	httpClient;
	/**
	* Initializes a new `MessagingApiBlobClient`.
	*
	* @param config Configuration for this API client.
	* @param config.baseURL The base URL for requests. Defaults to `https://api-data.line.me`.
	* @param config.channelAccessToken The channel access token used for authorization.
	* @param config.defaultHeaders Extra headers merged into every request.
	*
	* @example
	* const client = new MessagingApiBlobClient({
	*   channelAccessToken: process.env.LINE_CHANNEL_ACCESS_TOKEN!,
	* });
	*/
	constructor(config) {
		const baseURL = config.baseURL || "https://api-data.line.me";
		const defaultHeaders = mergeHeaders(config.defaultHeaders, { Authorization: "Bearer " + config.channelAccessToken });
		this.httpClient = new HTTPFetchClient({
			defaultHeaders,
			baseURL
		});
	}
	/**
	* Download image, video, and audio data sent from users.
	* Calls `GET https://api-data.line.me/v2/bot/message/{messageId}/content`.
	* To inspect the HTTP status code or response headers, use {@link getMessageContentWithHttpInfo}.
	* @param messageId Message ID of video or audio
	* @returns A promise resolving to the response body.
	* @see <a href="https://developers.line.biz/en/reference/messaging-api/#get-content">LINE Developers documentation</a>
	*/
	async getMessageContent(messageId) {
		return (await this.getMessageContentWithHttpInfo(messageId)).body;
	}
	/**
	* Download image, video, and audio data sent from users.
	* Calls `GET https://api-data.line.me/v2/bot/message/{messageId}/content`.
	* This method returns the response body together with the underlying `httpResponse`.
	* @param messageId Message ID of video or audio
	* @returns A promise resolving to the response body together with the underlying `httpResponse`.
	* @see <a href="https://developers.line.biz/en/reference/messaging-api/#get-content">LINE Developers documentation</a>
	*/
	async getMessageContentWithHttpInfo(messageId) {
		const requestPath = buildPath("/v2/bot/message/{messageId}/content", { messageId });
		const response = await this.httpClient.get(requestPath);
		return {
			httpResponse: response,
			body: convertResponseToReadable(response)
		};
	}
	/**
	* Get a preview image of the image or video
	* Calls `GET https://api-data.line.me/v2/bot/message/{messageId}/content/preview`.
	* To inspect the HTTP status code or response headers, use {@link getMessageContentPreviewWithHttpInfo}.
	* @param messageId Message ID of image or video
	* @returns A promise resolving to the response body.
	* @see <a href="https://developers.line.biz/en/reference/messaging-api/#get-image-or-video-preview">LINE Developers documentation</a>
	*/
	async getMessageContentPreview(messageId) {
		return (await this.getMessageContentPreviewWithHttpInfo(messageId)).body;
	}
	/**
	* Get a preview image of the image or video
	* Calls `GET https://api-data.line.me/v2/bot/message/{messageId}/content/preview`.
	* This method returns the response body together with the underlying `httpResponse`.
	* @param messageId Message ID of image or video
	* @returns A promise resolving to the response body together with the underlying `httpResponse`.
	* @see <a href="https://developers.line.biz/en/reference/messaging-api/#get-image-or-video-preview">LINE Developers documentation</a>
	*/
	async getMessageContentPreviewWithHttpInfo(messageId) {
		const requestPath = buildPath("/v2/bot/message/{messageId}/content/preview", { messageId });
		const response = await this.httpClient.get(requestPath);
		return {
			httpResponse: response,
			body: convertResponseToReadable(response)
		};
	}
	/**
	* Verify the preparation status of a video or audio for getting
	* Calls `GET https://api-data.line.me/v2/bot/message/{messageId}/content/transcoding`.
	* To inspect the HTTP status code or response headers, use {@link getMessageContentTranscodingByMessageIdWithHttpInfo}.
	* @param messageId Message ID of video or audio
	* @returns A promise resolving to the response body.
	* @see <a href="https://developers.line.biz/en/reference/messaging-api/#verify-video-or-audio-preparation-status">LINE Developers documentation</a>
	*/
	async getMessageContentTranscodingByMessageId(messageId) {
		return (await this.getMessageContentTranscodingByMessageIdWithHttpInfo(messageId)).body;
	}
	/**
	* Verify the preparation status of a video or audio for getting
	* Calls `GET https://api-data.line.me/v2/bot/message/{messageId}/content/transcoding`.
	* This method returns the response body together with the underlying `httpResponse`.
	* @param messageId Message ID of video or audio
	* @returns A promise resolving to the response body together with the underlying `httpResponse`.
	* @see <a href="https://developers.line.biz/en/reference/messaging-api/#verify-video-or-audio-preparation-status">LINE Developers documentation</a>
	*/
	async getMessageContentTranscodingByMessageIdWithHttpInfo(messageId) {
		const requestPath = buildPath("/v2/bot/message/{messageId}/content/transcoding", { messageId });
		const res = await this.httpClient.get(requestPath);
		const text = await res.text();
		return {
			httpResponse: res,
			body: text ? JSON.parse(text) : null
		};
	}
	/**
	* Download rich menu image.
	* Calls `GET https://api-data.line.me/v2/bot/richmenu/{richMenuId}/content`.
	* To inspect the HTTP status code or response headers, use {@link getRichMenuImageWithHttpInfo}.
	* @param richMenuId ID of the rich menu with the image to be downloaded
	* @returns A promise resolving to the response body.
	* @see <a href="https://developers.line.biz/en/reference/messaging-api/#download-rich-menu-image">LINE Developers documentation</a>
	*/
	async getRichMenuImage(richMenuId) {
		return (await this.getRichMenuImageWithHttpInfo(richMenuId)).body;
	}
	/**
	* Download rich menu image.
	* Calls `GET https://api-data.line.me/v2/bot/richmenu/{richMenuId}/content`.
	* This method returns the response body together with the underlying `httpResponse`.
	* @param richMenuId ID of the rich menu with the image to be downloaded
	* @returns A promise resolving to the response body together with the underlying `httpResponse`.
	* @see <a href="https://developers.line.biz/en/reference/messaging-api/#download-rich-menu-image">LINE Developers documentation</a>
	*/
	async getRichMenuImageWithHttpInfo(richMenuId) {
		const requestPath = buildPath("/v2/bot/richmenu/{richMenuId}/content", { richMenuId });
		const response = await this.httpClient.get(requestPath);
		return {
			httpResponse: response,
			body: convertResponseToReadable(response)
		};
	}
	/**
	* Upload rich menu image
	* Calls `POST https://api-data.line.me/v2/bot/richmenu/{richMenuId}/content`.
	* To inspect the HTTP status code or response headers, use {@link setRichMenuImageWithHttpInfo}.
	* @param richMenuId The ID of the rich menu to attach the image to
	* @param body
	* @returns A promise resolving to the response body.
	* @see <a href="https://developers.line.biz/en/reference/messaging-api/#upload-rich-menu-image">LINE Developers documentation</a>
	*/
	async setRichMenuImage(richMenuId, body) {
		return (await this.setRichMenuImageWithHttpInfo(richMenuId, body)).body;
	}
	/**
	* Upload rich menu image
	* Calls `POST https://api-data.line.me/v2/bot/richmenu/{richMenuId}/content`.
	* This method returns the response body together with the underlying `httpResponse`.
	* @param richMenuId The ID of the rich menu to attach the image to
	* @param body
	* @returns A promise resolving to the response body together with the underlying `httpResponse`.
	* @see <a href="https://developers.line.biz/en/reference/messaging-api/#upload-rich-menu-image">LINE Developers documentation</a>
	*/
	async setRichMenuImageWithHttpInfo(richMenuId, body) {
		const requestPath = buildPath("/v2/bot/richmenu/{richMenuId}/content", { richMenuId });
		const params = body;
		const res = await this.httpClient.postBinaryContent(requestPath, params);
		const text = await res.text();
		return {
			httpResponse: res,
			body: text ? JSON.parse(text) : null
		};
	}
};
//#endregion
//#region extensions/line/src/webhook.ts
const LINE_WEBHOOK_MAX_RAW_BODY_BYTES = 64 * 1024;
function readRawBody(req) {
	const rawBody = req.rawBody ?? (typeof req.body === "string" || Buffer.isBuffer(req.body) ? req.body : null);
	if (!rawBody) return null;
	return Buffer.isBuffer(rawBody) ? rawBody.toString("utf-8") : rawBody;
}
function parseWebhookBody(rawBody) {
	if (!rawBody) return null;
	return parseLineWebhookBody(rawBody);
}
function createLineWebhookMiddleware(options) {
	const { channelSecret, onEvents, runtime } = options;
	return async (req, res, _next) => {
		try {
			const signature = req.headers["x-line-signature"];
			if (!signature || typeof signature !== "string") {
				res.status(400).json({ error: "Missing X-Line-Signature header" });
				return;
			}
			const rawBody = readRawBody(req);
			if (!rawBody) {
				res.status(400).json({ error: "Missing raw request body for signature verification" });
				return;
			}
			if (Buffer.byteLength(rawBody, "utf-8") > LINE_WEBHOOK_MAX_RAW_BODY_BYTES) {
				res.status(413).json({ error: "Payload too large" });
				return;
			}
			if (!validateLineSignature(rawBody, signature, channelSecret)) {
				logVerbose("line: webhook signature validation failed");
				res.status(401).json({ error: "Invalid signature" });
				return;
			}
			const body = parseWebhookBody(rawBody);
			if (!body) {
				res.status(400).json({ error: "Invalid webhook payload" });
				return;
			}
			if (body.events && body.events.length > 0) {
				logVerbose(`line: received ${body.events.length} webhook events`);
				await onEvents(body);
			}
			res.status(200).json({ status: "ok" });
		} catch (err) {
			runtime?.error?.(danger(`line webhook error: ${String(err)}`));
			if (!res.headersSent) res.status(500).json({ error: "Internal server error" });
		}
	};
}
function startLineWebhook(options) {
	const channelSecret = typeof options.channelSecret === "string" ? options.channelSecret.trim() : "";
	if (!channelSecret) throw new Error("LINE webhook mode requires a non-empty channel secret. Set channels.line.channelSecret in your config.");
	return {
		path: options.path ?? "/line/webhook",
		handler: createLineWebhookMiddleware({
			channelSecret,
			onEvents: options.onEvents,
			runtime: options.runtime
		})
	};
}
//#endregion
//#region extensions/line/src/rich-menu.ts
const graphemeSegmenter = new Intl.Segmenter(void 0, { granularity: "grapheme" });
function getClient(opts) {
	const account = resolveLineAccount({
		cfg: opts.cfg,
		accountId: opts.accountId
	});
	return new MessagingApiClient({ channelAccessToken: resolveLineChannelAccessToken(opts.channelAccessToken, account) });
}
function getBlobClient(opts) {
	const account = resolveLineAccount({
		cfg: opts.cfg,
		accountId: opts.accountId
	});
	return new MessagingApiBlobClient({ channelAccessToken: resolveLineChannelAccessToken(opts.channelAccessToken, account) });
}
function truncateGraphemes(input, maxLength) {
	let result = "";
	let count = 0;
	for (const { segment } of graphemeSegmenter.segment(input)) {
		if (count >= maxLength) break;
		result += segment;
		count += 1;
	}
	return result;
}
async function createRichMenu(menu, opts) {
	const client = getClient(opts);
	const richMenuRequest = {
		size: menu.size,
		selected: menu.selected ?? false,
		name: truncateGraphemes(menu.name, 300),
		chatBarText: truncateGraphemes(menu.chatBarText, 14),
		areas: menu.areas
	};
	const response = await client.createRichMenu(richMenuRequest);
	if (opts.verbose) logVerbose(`line: created rich menu ${response.richMenuId}`);
	return response.richMenuId;
}
async function uploadRichMenuImage(richMenuId, imagePath, opts) {
	const blobClient = getBlobClient(opts);
	const media = await loadWebMediaRaw(imagePath, { localRoots: opts.mediaLocalRoots ?? getAgentScopedMediaLocalRoots(opts.cfg) });
	const contentType = media.contentType === "image/png" || media.contentType === "image/jpeg" ? media.contentType : mimeTypeFromFilePath(imagePath) === "image/png" ? "image/png" : "image/jpeg";
	const imageBytes = new ArrayBuffer(media.buffer.byteLength);
	new Uint8Array(imageBytes).set(media.buffer);
	await blobClient.setRichMenuImage(richMenuId, new Blob([imageBytes], { type: contentType }));
	if (opts.verbose) logVerbose(`line: uploaded image to rich menu ${richMenuId}`);
}
async function setDefaultRichMenu(richMenuId, opts) {
	await getClient(opts).setDefaultRichMenu(richMenuId);
	if (opts.verbose) logVerbose(`line: set default rich menu to ${richMenuId}`);
}
async function cancelDefaultRichMenu(opts) {
	await getClient(opts).cancelDefaultRichMenu();
	if (opts.verbose) logVerbose("line: cancelled default rich menu");
}
async function getDefaultRichMenuId(opts) {
	const client = getClient(opts);
	try {
		return (await client.getDefaultRichMenuId()).richMenuId ?? null;
	} catch {
		return null;
	}
}
async function getRichMenuIdOfUser(userId, opts) {
	const client = getClient(opts);
	try {
		return (await client.getRichMenuIdOfUser(userId)).richMenuId ?? null;
	} catch {
		return null;
	}
}
async function getRichMenuList(opts) {
	return (await getClient(opts).getRichMenuList()).richmenus ?? [];
}
async function getRichMenu(richMenuId, opts) {
	const client = getClient(opts);
	try {
		return await client.getRichMenu(richMenuId);
	} catch {
		return null;
	}
}
async function deleteRichMenu(richMenuId, opts) {
	await getClient(opts).deleteRichMenu(richMenuId);
	if (opts.verbose) logVerbose(`line: deleted rich menu ${richMenuId}`);
}
async function createRichMenuAlias(richMenuId, aliasId, opts) {
	await getClient(opts).createRichMenuAlias({
		richMenuId,
		richMenuAliasId: aliasId
	});
	if (opts.verbose) logVerbose(`line: created alias ${aliasId} for rich menu ${richMenuId}`);
}
async function deleteRichMenuAlias(aliasId, opts) {
	await getClient(opts).deleteRichMenuAlias(aliasId);
	if (opts.verbose) logVerbose(`line: deleted alias ${aliasId}`);
}
function createGridLayout(height, actions) {
	const colWidth = Math.floor(2500 / 3);
	const rowHeight = Math.floor(height / 2);
	return [
		{
			bounds: {
				x: 0,
				y: 0,
				width: colWidth,
				height: rowHeight
			},
			action: actions[0]
		},
		{
			bounds: {
				x: colWidth,
				y: 0,
				width: colWidth,
				height: rowHeight
			},
			action: actions[1]
		},
		{
			bounds: {
				x: colWidth * 2,
				y: 0,
				width: colWidth,
				height: rowHeight
			},
			action: actions[2]
		},
		{
			bounds: {
				x: 0,
				y: rowHeight,
				width: colWidth,
				height: rowHeight
			},
			action: actions[3]
		},
		{
			bounds: {
				x: colWidth,
				y: rowHeight,
				width: colWidth,
				height: rowHeight
			},
			action: actions[4]
		},
		{
			bounds: {
				x: colWidth * 2,
				y: rowHeight,
				width: colWidth,
				height: rowHeight
			},
			action: actions[5]
		}
	];
}
function createDefaultMenuConfig() {
	return {
		size: {
			width: 2500,
			height: 843
		},
		selected: false,
		name: "Default Menu",
		chatBarText: "Menu",
		areas: createGridLayout(843, [
			messageAction("Help", "/help"),
			messageAction("Status", "/status"),
			messageAction("Settings", "/settings"),
			messageAction("About", "/about"),
			messageAction("Feedback", "/feedback"),
			messageAction("Contact", "/contact")
		])
	};
}
//#endregion
export { DEFAULT_ACCOUNT_ID, LineChannelConfigSchema, LineConfigSchema, buildChannelConfigSchema, buildComputedAccountStatusSnapshot, buildTemplateMessageFromPayload, buildTokenChannelStatusSummary, cancelDefaultRichMenu, clearAccountEntryFields, convertCodeBlockToFlexBubble, convertLinksToFlexBubble, convertTableToFlexBubble, createActionCard, createAgendaCard, createAppleTvRemoteCard, createAudioMessage, createButtonMenu, createButtonTemplate, createCarousel, createCarouselColumn, createConfirmTemplate, createDefaultMenuConfig, createDeviceControlCard, createEventCard, createFlexMessage, createGridLayout, createImageCard, createImageCarousel, createImageCarouselColumn, createImageMessage, createInfoCard, createLineNodeWebhookHandler, createLineWebhookMiddleware, createLinkMenu, createListCard, createLocationMessage, createMediaPlayerCard, createNotificationBubble, createProductCarousel, createQuickReplyItems, createReceiptCard, createRichMenu, createRichMenuAlias, createTemplateCarousel, createTextMessageWithQuickReplies, createVideoMessage, createYesNoConfirm, datetimePickerAction, deleteRichMenu, deleteRichMenuAlias, downloadLineMedia, extractCodeBlocks, extractLinks, extractMarkdownTables, firstDefined, formatDocsLink, getDefaultRichMenuId, getRichMenu, getRichMenuIdOfUser, getRichMenuList, getUserDisplayName, getUserProfile, hasLineDirectives, hasMarkdownToConvert, listLineAccountIds, messageAction, monitorLineProvider, normalizeAccountId, normalizeAllowFrom, parseLineDirectives, parseLineWebhookBody, postbackAction, probeLineBot, processLineMessage, pushFlexMessage, pushImageMessage, pushLocationMessage, pushMessageLine, pushMessagesLine, pushTemplateMessage, pushTextMessageWithQuickReplies, readLineWebhookRequestBody, replyMessageLine, resolveDefaultLineAccountId, resolveExactLineGroupConfigKey, resolveLineAccount, resolveLineChannelAccessToken, resolveLineGroupConfigEntry, resolveLineGroupLookupIds, resolveLineGroupsConfig, sendMessageLine, setDefaultRichMenu, setLineRuntime, setSetupChannelEnabled, showLoadingAnimation, splitSetupEntries, startLineWebhook, stripMarkdown, toFlexMessage, uploadRichMenuImage, uriAction, validateLineSignature };
